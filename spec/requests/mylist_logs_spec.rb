require 'spec_helper'

describe "MylistLogs" do
  describe "GET /mylist_logs" do
    it "works! (now write some real specs)" do
      get mylist_logs_path
      expect(response.status).to be(200)
    end
  end
end
